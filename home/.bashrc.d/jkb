# jkbuster knock Aliases
if [ -f $HOME/.knock.seq ]; then
  if [ $(uname) == "Linux" ] && [ -x $HOME/.knock.d/knock.linux ]; then
    alias jkb='for i in $(cat $HOME/.knock.seq);do $HOME/.knock.d/knock.linux jkbuster.com $i;done; ssh jkbuster.com'
  elif [ $(uname) == "Darwin" ] && [ -x $HOME/.knock.d/knock.osx ]; then
    alias jkb='for i in $(cat $HOME/.knock.seq);do $HOME/.knock.d/knock.osx jkbuster.com $i;done; ssh jkbuster.com'
  fi
fi
